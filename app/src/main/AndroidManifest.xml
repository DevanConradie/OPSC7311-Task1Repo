<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.opsc7311_task1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OPSC7311Task1"
        tools:targetApi="31">

        <!-- ChecklistActivity -->
        <activity
            android:name=".ChecklistActivity"
            android:exported="false" />

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false">
            <!-- Intent filter and other activity configuration if needed -->
        </activity>

        <!-- TimesheetActivity -->
        <activity
            android:name=".TimesheetActivity"
            android:exported="false">
            <!-- Intent filter and other activity configuration if needed -->
        </activity>

        <!-- CategoryDetailActivity -->
        <activity
            android:name=".CategoryDetailActivity"
            android:exported="false">
            <!-- Intent filter and other activity configuration if needed -->
        </activity>

    </application>

</manifest>
